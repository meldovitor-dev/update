import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.oi.tecnicovirtual',
  appName: 'front-end v3',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
